{
  "name": "pure-html-ts-project",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch db.json --port 3001",
    "dev": "concurrently \"npm run server\" \"live-server\"",
    "build": "tsc",
    "start": "npm run build && live-server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "live-server": "^1.2.2",
    "typescript": "^5.8.3"
  }
}
